export function ColorBox({ color }) {
  const stles = {
    height: "25px",
    width: "250px",
    background: color,
    margin: "5px 0px",
  };
  return (
    <div style={stles}>

    </div>
  );
}
